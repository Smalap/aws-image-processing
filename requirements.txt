# Runtime dependencies for the Lambda handler.
#
# In AWS, Pillow is supplied by the `pillow-layer` Lambda layer and boto3 is
# preinstalled in the Lambda Python runtime — neither is packaged in the
# deployment zip. This file exists so CI can install them for linting and
# import checks, and so the dependency set is documented.

Pillow>=11.0.0
boto3>=1.34.0
